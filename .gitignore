node_modules

# CDK asset staging directory
.cdk.staging
cdk.out

# exclude vids
*.mp4

# Local dev env config (generated by CDK in production)
frontend/scripts/env.js

# local scripts are needed to fix stuff here and there, but we don't want to commit them
scripts

# Deployers' real config (with domain, email, etc.)
site.config.json

# Production brand overrides (custom frontend/docs, img, etc.) — never commit these
brand-override/

# AWS account IDs, hosted zone IDs, etc. generated by CDK
cdk.context.json

#ffmpeg
ffmpeg-layer

# Ensure /frontend/scripts is included
!frontend/scripts